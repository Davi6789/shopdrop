Tag 2 Di.31.03.26

Tag 2 bauen das Frontend — Bun + React + Tailwind + TypeScript. Du wirst dann zum ersten Mal deine Pflanzen im Browser sehen 🌿

cd ~/Antonio/Projekt/shopdrop/frontend
bun create vite . --template react-ts

bun install
bun add -d tailwindcss @tailwindcss/vite
Jetzt Tailwind konfigurieren. Öffne vite.config.ts und füge in zeile pluging tailwindcss und import tailwindcss from '@tailwindcss/vite' ein.

Dann src/index.css — alles löschen und ersetzen mit:
@import "tailwindcss";

bun run dev dann sieht ➜  Local: http://localhost:5173/

Schritt 2 — API Client + ProductList
- API-Client anlegen und Erstelle src/api/client.ts
- TypeScript-Typen anlegen. Erstelle src/types/index.ts
- ProductList-Seite. Erstelle src/pages/Home.tsx

Schritt 3 — ProductCard Komponente
- Erstelle src/components/ProductCard.tsx
- Jetzt src/App.tsx bearbeiten, löschen alles und fängt neu an

wenn man interface benutzt muss man mit import type zu typisieren

Schritt 4: Warenkorb mit Context.
Der Warenkorb-State muss in der ganzen App verfügbar sein (ProductCard + Navbar). 
Dafür nutzen wir React Context.
- Erstelle src/context/CartContext.tsx
    import type { Product } from "../types";
    import type { CartItem } from "../types";


- src/main.tsx updaten damit der Provider die ganze App umschließt
    import { CartProvider } from './context/CartContext.tsx'

   <CartProvider>
      <App />
    </CartProvider>

- "In den Warenkorb" Button in src/components/ProductCard.tsx verbinden
    import type { useCart } from '../context/CartContext';
       const { addToCart } = useCart();

         <button 
          onClick={() => addToCart(product)}

- die Navbar in src/App.tsx mit dem Warenkorb-Zähler:
    import type { useCart } from './context/CartContext';

    const { totalItems } = useCart();
    ... flex justify-between items-center"></nav>

Schritt 5 — Cart Drawer
- Erstelle src/components/CartDrawer.tsx
- src/App.tsx updaten damit der Drawer öffnet wenn man auf den Warenkorb klickt
    //src/App.tsx
    import { useState } from 'react';
    import CartDrawer from './components/CartDrawer';
    const [cartOpen, setCartOpen] = useState(false);

    <button
          onClick={() => setCartOpen(true)}
          className="flex items-center gap-2 bg-green-700 hover:bg-green-600 px-4 py-2 rounded-xl transition-colors"
        >
     
      <CartDrawer open={cartOpen} onClose={() => setCartOpen(false)} />   

Tag 2 — Abgeschlossen! ✅
✅ Schritt 1 — Bun + Vite + React + Tailwind Setup
✅ Schritt 2 — API Client + ProductList
✅ Schritt 3 — ProductCard Komponente
✅ Schritt 4 — Cart Context (globaler State)
✅ Schritt 5 — Cart Drawer mit + / - / Gesamt